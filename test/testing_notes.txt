The independence of test cases is a key principle of unit testing.

test suite = describe keyword 
test case = it keyword

Example Mocha Test:

describe('Math Operations', function () {
  it('should add two numbers', function () {
    assert.equal(2 + 2, 4);
  });
});


Example Chai Assertions:

// Using assert style
assert.equal(2 + 2, 4);

// Using expect style
expect(2 + 2).to.equal(4);

// Using should style
(2 + 2).should.equal(4);

npx img-cli compress .\myphoto_tbh.png 
npx img-cli compress .\myphoto_tbh.png -q 0 
npx img-cli compress .\myphoto_tbh.png -q 80 
npx img-cli compress .\myphoto_tbh.png -q 

npx img-cli rotate .\myphoto_tbh.jpeg 30
npx img-cli rotate .\myphoto_tbh.jpeg
npx img-cli rotate .\myphoto_tbh.tiff //does not exist
